#include <stdio.h>
#include <ctype.h>

/**
 * main - entry point
 *
 * return: always 0 (success)
 */

int main(void)
{
int Lowercase = 'a';
while (lowercae <= 'z')
}
putchar(lowercase);
lowercase += 1;
}
putchar('\n');
return (0);
}
